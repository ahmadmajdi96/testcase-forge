# Anchored to the build-context root so nested dirs like src/coverage are NOT
# excluded (a bare "coverage" pattern also matches src/coverage).
node_modules
.git
.env
*.log
/dist
/tests
/scripts
/coverage
/artifacts
/data
